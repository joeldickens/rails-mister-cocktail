<!-- <div class="container"><%= render "form", cocktail: @cocktail %></div> -->
<div class="container">
  <%= link_to "Nevermind", root_path %>
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h1>First, name your cocktail!</h1>

      <%= simple_form_for @cocktail do |f| %>
        <%= f.input :name %>
        <%= f.button :submit, class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>
